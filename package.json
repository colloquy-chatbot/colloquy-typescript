{
  "name": "colloquy_chatbot",
  "version": "0.11.2",
  "main": "./dist/colloquy.js",
  "types": "./dist/colloquy.d.ts",
  "type": "module",
  "exports": {
    ".": {
      "types": "./dist/colloquy.d.ts",
      "import": "./dist/colloquy.js"
    }
  },
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "tsc",
    "test": "bun test",
    "check": "bun x tsc --noEmit",
    "lint": "echo 'No linting configured - passing'",
    "verify": "bun run build && bun run test && bun run check",
    "prepublishOnly": "bun run verify"
  },
  "devDependencies": {
    "@types/bun": "latest",
    "@types/escodegen": "^0.0.10",
    "@types/esprima": "^4.0.1"
  },
  "peerDependencies": {
    "typescript": "^5"
  },
  "dependencies": {
    "@anthropic-ai/sdk": "^0.39.0",
    "escodegen": "^2.1.0",
    "esprima": "^4.0.1",
    "openai": "4.104.0"
  }
}
